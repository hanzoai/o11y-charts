{{- if .Values.config.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "o11y-otel-gateway.fullname" . }}
  labels:
    {{- include "o11y-otel-gateway.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- omit .Values.config "create" | toYaml | nindent 4 }}
{{- end }}